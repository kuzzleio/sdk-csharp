name: Collection#CreateAsync
description: Create a new collection.
hooks:
  before: curl -X POST kuzzle:7512/nyc-open-data/_create
  after:
template: default
expected: Collection successfully created
